package com.qualityops.events;

/** Coarse CPU / memory / disk envelope the Worker maps to concrete container
 *  {@code HostConfig} limits. */
public enum RepoResourceProfile { SMALL, MEDIUM, LARGE }
